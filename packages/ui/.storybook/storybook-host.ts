// Placeholder entry referenced by the `eagami-ui-storybook:build` target in
// angular.json. The Storybook builders point at that target via
// `browserTarget` to satisfy `@storybook/angular`'s legacy-build-options
// guard; Storybook bootstraps its own runtime, so this file never executes.
export {};
