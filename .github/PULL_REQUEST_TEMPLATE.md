<!--
The PR title must be the branch name: `website-vX.Y.Z` for a website-only change,
or `ui-vA.B.C-website-vX.Y.Z` for any library change (always paired with a website
bump). Plain `vX.Y.Z` and standalone `ui-vX.Y.Z` are rejected by CI.

This body is the release notes: paste this version's CHANGELOG entries verbatim.
A combined PR includes both, each under its own heading (`## @eagami/ui vA.B.C`
and `## Website vX.Y.Z`).
-->
